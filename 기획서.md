# Q&A IBS 묵상 서비스 기획서

## 1. 서비스 개요

**서비스명(가안)**: Q&A IBS 묵상

**목적**: 그리스도인들이 성경을 재미있고 신나게 묵상하고 삶에 적용하며, 이를 통해 하나님을 사랑하고 그리스도의 장성한 분량에 이르도록 돕는다.

**핵심 방법론**: 귀납적 성경 공부(Inductive Bible Study, IBS) — 답을 알려주는 것이 아니라, 스스로 본문을 관찰·해석·적용하며 진리를 발견하도록 돕는다.

## 2. 타겟 사용자

- IBS 방식으로 묵상하고 싶은 그리스도인 (연령 무관, 어린이 포함 가능)
- 성경의 시대적 배경이 낯설어 스스로 배경 조사를 하기 어려운 사람
- 모바일로 짧은 시간에도 묵상을 이어가고 싶은 사람

## 3. 페이지 구성 및 내비게이션

**내비게이션 방식**: 모바일은 하단 탭바, 데스크톱은 상단 네비게이션(반응형 전환)

| 탭 | 화면 내용 |
|---|---|
| 홈 | 오늘 묵상 시작 CTA, 진행 현황 |
| 개관 | 책 선택 → BRI·key verse·문단 구조 (AI 생성) |
| 묵상 | 7단계 진행: 기도 → 소리내어 읽기 → 관찰 → 배경 → 해석 → 성경적 진리 → 개인 적용 |
| 기록 | 지난 묵상 다시보기 |

**요구사항**: 묵상 진행 중에도 해당 본문의 BRI나 전체 문단 구조로 언제든 이동할 수 있어야 한다 (예: 묵상 화면 내 "개관 보기" 버튼/링크).

## 4. 핵심 기능

1. 성경책 개관 자동 생성 (BRI, key verse, 문단 구조)
2. 본문 조회 (개역한글 데이터 기반, 장·절 입력)
3. 7단계 IBS 묵상 흐름 (사용자 직접 입력)
4. 대화형 AI 가이드 — 사용자의 답변을 반영해 후속 질문을 이어가는 방식
5. 오늘의 묵상 본문 제시 — 미리 정해둔 reading plan 데이터를 기반으로 홈 화면에서 오늘 날짜에 해당하는 본문을 안내
6. 묵상 나눔 공유 — 완료한 묵상 내용을 정리된 텍스트로 복사하거나(데스크톱) 모바일 공유 시트(카카오톡 등)로 바로 내보내는 버튼

## 5. AI 기능 입력/출력/실패 처리 기준

AI 제공자: Claude (Anthropic API), 3개 기능 모두 동일

### 5-1. 책 개관 생성
- **입력**: 성경책 이름
- **출력**: BRI(저자·연대·목적 등), key verse, 문단 구조 (JSON)
- **실패 처리**: 책 미선택 시 "책을 선택해주세요" / API 오류 시 "잠시 후 다시 시도해주세요" / 응답 지연 시 로딩 표시 후 안내

### 5-2. 문단 구조 상세 조회
- **입력**: 책 이름 + (선택) 특정 장
- **출력**: 해당 장의 단락 구분과 각 단락 요약
- **실패 처리**: 동일 기준 적용

### 5-3. Q&A IBS 대화형 묵상 가이드
- **입력**: 현재 단계(관찰/해석/진리/적용), 본문(장·절), 이전 단계까지의 사용자 답변
- **출력**: 사용자의 답변에 이어지는 후속 질문 1개 (정답을 직접 제시하지 않음)
- **실패 처리**: 답변 미입력 시 "답변을 입력해주세요" / API 오류·지연 시 동일 기준 적용
- **원칙**: 관찰·해석·진리·적용 단계는 AI가 질문만 하고 사용자가 스스로 답을 찾도록 유도. 개관·배경 단계는 AI가 정보를 직접 제공.

## 6. 부가 기능 동작 방식

### 6-1. 오늘의 묵상 본문 제시
- `data/reading-plan.json`에 일차별(또는 날짜별) 본문 목록을 미리 정의
- 홈 화면 접속 시 오늘 날짜에 해당하는 본문을 자동 표시, "오늘의 본문으로 묵상 시작하기" 클릭 시 해당 본문이 채워진 묵상 화면으로 이동
- AI 호출 없이 정적 데이터 조회로 동작

### 6-2. 묵상 나눔 공유
- 묵상 완료 시 결과 화면에 "나눔 공유" 버튼 표시
- 모바일: Web Share API(`navigator.share`)로 카카오톡 등 설치된 공유 대상 선택 창 호출
- 데스크톱(또는 공유 API 미지원 환경): 클립보드 복사로 대체 동작
- 공유되는 텍스트는 본문 출처와 관찰·해석·성경적 진리·적용 답변을 정리한 형식
- 별도 데이터베이스나 로그인 없이 동작 (개인 기기 내 완료된 묵상을 텍스트로 내보내는 방식)

### 6-3. 묵상 기록 저장 및 조회
- 묵상 완료 화면에 "이 묵상 저장하기" 버튼 표시 → 클릭 시 7단계 전체 기록(단계별 질문·답변, 책, 장·절)을 서버에 저장
- 로그인 기능 없이, 브라우저별 익명 ID(localStorage에 저장되는 UUID)로 "누구의 기록인지"를 구분 → 같은 기기·브라우저에서만 자신의 기록이 보임(기기·브라우저를 바꾸면 이전 기록 접근 불가)
- "묵상 기록" 메뉴에서 저장된 기록을 최신순으로 목록 표시, 카드를 클릭하면 해당 묵상의 단계별 질문·답변이 펼쳐짐
- **실패 처리**: 저장 실패 시 "저장에 실패했습니다. 잠시 후 다시 시도해주세요." / 기록이 하나도 없을 때 "아직 저장된 묵상 기록이 없습니다."

## 7. 기술 스택

- 프론트엔드: HTML/CSS/JavaScript (프레임워크 미사용)
- 백엔드: Vercel Serverless Functions (Python)
- 데이터베이스: Vercel Postgres (Neon) — 저장된 묵상 기록 보관
- AI: Claude API (Anthropic)
- 본문 데이터: 개역한글 JSON (자체 보유 데이터)
- 사용자 식별: 로그인 없이 브라우저별 익명 ID(localStorage)
- 배포: GitHub + Vercel